# ============================================================
# AI-Powered Prescription Discrepancy Detection System
# Backend Requirements — Python 3.11+
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9          # file upload support
httpx==0.27.0                    # async HTTP client

# --- Auth ---
python-jose[cryptography]==3.3.0 # JWT encode/decode
passlib[bcrypt]==1.7.4           # password hashing
bcrypt==4.1.3

# --- Database ---
sqlalchemy==2.0.30
alembic==1.13.1                  # DB migrations
asyncpg==0.29.0                  # async PostgreSQL driver
psycopg2-binary==2.9.9           # sync fallback / Alembic

# --- Caching / Queue ---
redis==5.0.4
celery==5.4.0                    # async job queue
flower==2.0.1                    # Celery monitoring (optional)

# --- AWS ---
# boto3==1.34.114                  # S3 image storage (uncomment if using S3)
botocore==1.34.114

# --- OpenAI / LLM ---
openai==1.30.1                   # GPT-4o Vision + embeddings
tiktoken==0.7.0                  # token counting

# --- LangChain + RAG ---
langchain==0.2.3
langchain-openai==0.1.8
langchain-community==0.2.3
langchain-core==0.2.5
faiss-cpu==1.8.0                 # vector store (swap to faiss-gpu on GPU instances)

# --- RAG Retrieval ---
pypdf==4.2.0
pdfminer.six==20231228
nltk==3.8.1
regex==2024.5.15

# --- Drug Validation APIs ---
requests==2.32.3                 # sync HTTP for RxNorm / OpenFDA calls
aiohttp==3.9.5                   # async HTTP variant

# --- ML ---
scikit-learn==1.5.0
xgboost==2.0.3
shap==0.45.1                     # model explainability
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2                    # model serialization

# --- NLP / Text Processing ---
rapidfuzz==3.9.3                 # fuzzy drug name matching
ftfy==6.2.0                      # fix broken unicode / OCR artifacts
unidecode==1.3.8                 # normalize accented characters

# ---------------------------------------------------------------
# NEW LIBRARIES (added in latest upgrade)
# ---------------------------------------------------------------

# --- PDF Input Support (NEW) ---
# Converts uploaded PDF prescriptions → images before GPT-4o Vision
PyMuPDF==1.24.5                  # PRIMARY: fast PDF→image, zero system deps (import as fitz)
pdf2image==1.17.0                # FALLBACK: PDF→image via poppler (needs poppler installed)
Pillow==10.3.0                   # image manipulation — required by pdf2image + base64 encoding

# --- PDF Report Generation (CHANGED: WeasyPrint → reportlab) ---
# WeasyPrint required GTK/cairo system libraries which are unavailable on most servers.
# reportlab is pure-Python, installs with pip, no system dependencies.
reportlab==4.2.2                 # PDF canvas + layout engine for audit reports

# ---------------------------------------------------------------
# REMOVED LIBRARIES
# ---------------------------------------------------------------
# weasyprint==62.3    — replaced by reportlab (GTK dependency removed)
# jinja2==3.1.4       — was only used for WeasyPrint HTML templates; no longer needed

# --- Data Validation (Pydantic) ---
pydantic==2.7.3
pydantic-settings==2.3.1

# --- Logging & Observability ---
structlog==24.2.0
sentry-sdk[fastapi]==2.5.0       # error tracking

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0                    # async test client for FastAPI
factory-boy==3.3.0               # test fixtures

# --- Dev / Linting (optional, remove in prod image) ---
ruff==0.4.7
mypy==1.10.0
pre-commit==3.7.1
